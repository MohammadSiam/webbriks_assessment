# Used by infra/docker-compose.db.yml (run `docker compose -f infra/docker-compose.db.yml up -d` from the repo root)

POSTGRES_USER=webbriks
POSTGRES_PASSWORD=webbriks
POSTGRES_DB=webbriks_kanban
POSTGRES_PORT=5433
